# PRD — 그림 맞히기 교육 게임 (가칭: **그려서 배워요!**)

## 1. 개요

| 항목 | 내용 |
| --- | --- |
| 한 줄 요약 | 방에 모인 3~10명이 돌아가며 그림을 그리고 맞히는 초등 교육용 실시간 멀티플레이 게임 |
| 대상 | 한국 초등학생(1~6학년), 교사·학부모 |
| 핵심 가치 | 교과 개념을 그림으로 표현하고 추론하며 자연스럽게 복습 |
| 콘셉트 | 학교 교실 — 초록 칠판에 분필로 그리는 느낌, 출석부·알림장(공책) UI |
| 캐릭터 | **AI 펭수** — 제시어 출제, 오답 격려, 힌트 제공, 정답 축하, 쉬운 설명 |
| 플랫폼 | 웹 (PC·태블릿·모바일 브라우저) |
| 배포/인프라 | Vercel (Next.js) + Neon (PostgreSQL) + OpenAI API |

## 2. 목표 / 비목표

**목표**
- 회원가입 없이 닉네임과 방 코드만으로 바로 참여
- 교과(주제)·학년 수준(난이도)에 맞는 제시어를 AI가 자동 출제
- 그림 그리는 과정이 다른 참가자에게 실시간(1초 이내)으로 보임
- 채팅으로 정답을 맞히고 대화, 정답자가 다음 출제자(리더)가 됨
- 라운드별·최종 순위 제공

**비목표 (1차 버전 제외)**
- 회원 계정, 전적 저장, 친구 기능
- 음성 채팅, 그림 저장/공유
- 교사용 관리 대시보드

## 3. 용어

| 용어 | 뜻 |
| --- | --- |
| 방 | 6자리 숫자 코드로 식별되는 게임 공간 (최대 10명) |
| 방장 | 방을 만든 사람. 게임 시작·다음 라운드·다시하기 권한 (나가면 자동 위임) |
| 리더(출제자) | 현재 그림을 그리는 사람. 주제 선택 권한을 가짐 |
| 문제 | 제시어 1개를 그리고 맞히는 단위 |
| 세트 | 같은 주제로 푸는 문제 5개 |
| 라운드 | 세트 3개(= 15문제). 끝나면 라운드 순위 발표 |
| 게임 | 라운드 3개(= 45문제). 끝나면 최종 순위 발표 |

## 4. 게임 흐름

```
[대기실] --방장 시작(3명 이상)--> [주제 선택] --리더가 주제·난이도 선택--> [AI 제시어 5개 생성]
   ^                                  ^                                          |
   |                                  |                                          v
   |                             (세트 2·3)                                 [그리기 & 맞히기]
   |                                  |                                          |
   |                                  +--- 5문제 끝 ---- [정답+설명 8초] <--------+ 정답 or 시간초과
   |                                                          |
   |                                                    3세트 끝
   |                                                          v
   +---- 방장 다시하기 ---- [최종 순위] <-- 3라운드 끝 -- [라운드 순위]
```

### 4.1 대기실
- 방 만들기: 닉네임 입력 → 6자리 방 코드 발급 → 초대 링크 복사
- 참가: 방 코드 입력 또는 링크 접속 → 닉네임 입력
- 인원: 최소 3명, 최대 10명 (10명 초과 시 입장 거부)
- 방장에게만 **게임 시작** 버튼 노출, 접속 중 인원이 3명 미만이면 비활성
- 첫 리더 = 방장

### 4.2 주제 선택
- 리더에게 주제 × 난이도 선택 화면 표시, 다른 참가자는 “○○님이 주제를 고르는 중” 대기
- 제한 시간 40초. 초과 시 무작위 주제 + ‘보통’ 난이도 자동 선택
- 선택 즉시 AI가 해당 세트의 제시어 5개와 제시어별 힌트 3개·설명을 한 번에 생성 (펭수 말투)

**주제 (초등 교육과정 기반)**

| 주제 | 예시 제시어 |
| --- | --- |
| 역사 | 거북선, 첨성대, 측우기, 고인돌, 한글 |
| 사회 | 신호등, 우체국, 투표, 지도, 시장 |
| 과학 | 화산, 무지개, 자석, 현미경, 광합성 |
| 문학(국어) | 흥부와 놀부, 해님 달님, 토끼전, 동시, 편지 |
| 영어 | apple, rainbow, library, umbrella (영어로 정답 입력) |
| 수학 | 삼각형, 시계, 분수, 각도기, 막대그래프 |
| 예체능(음악·미술·체육) | 리코더, 팔레트, 줄넘기, 탬버린, 태권도 |

**난이도**

| 난이도 | 수준 |
| --- | --- |
| 쉬움 | 1~2학년 |
| 보통 | 3~4학년 |
| 어려움 | 5~6학년 |

### 4.3 그리기 & 맞히기
- 리더 화면: 제시어 표시, 칠판 받침대의 그림 도구
  - **분필 색: 무지개 7색** (빨강·주황·노랑·초록·파랑·남색·보라, 칠판에서 잘 보이는 밝은 톤)
  - **굵기 4단계** (가늘게·보통·굵게·아주 굵게)
  - **지우개**: 부분 지우개(문지른 곳만, 굵기 적용) / 전체 지우개(칠판 전체) + 되돌리기
- 참가자 화면: 실시간 그림, 제시어 글자 수(예: `○ ○ ○ (3글자)`), 공개된 힌트(포스트잇), 남은 시간, 휴대폰에서는 칠판 바로 아래 정답 입력칸
- 제한 시간: 100초
- 채팅: 모든 입력은 대화이자 정답 시도
  - 정답 판정: 공백·문장부호 제거, 영문 대소문자 무시 후 제시어 또는 허용 별칭과 일치
  - 정답 메시지는 다른 사람에게 내용이 보이지 않고 “🎉 ○○님이 정답을 맞혔어요!”로 표시
  - 리더는 제시어가 포함된 채팅을 보낼 수 없음(차단 안내)
- **힌트**: 오답(리더 외 참가자 채팅) 누적 5회마다 **AI 펭수가 자동으로 힌트 1개씩 공개** (5회→1번째, 10회→2번째, 15회→3번째)
  - 오답이 3·8·13…회일 때 펭수가 격려, 한 글자 차이로 아깝게 틀리면 이름을 불러 “거의 맞았어!”
- 종료 조건
  - 정답자 발생 → 정답자가 다음 리더
  - 시간 초과 또는 리더 접속 끊김(20초) → 정답 공개, 참가 순서상 다음 사람이 리더
- **정답 공개 & 배움 카드 (8초)**: 정답 단어와 함께 AI가 만든 **초등학생 눈높이의 쉬운 설명(1~2문장)**을 모두에게 보여주고, 채팅창에도 `📘 알고 가요!` 메시지로 남김
  - 예) 첨성대 — “신라 시대에 별과 하늘을 관찰하려고 돌을 쌓아 만든 천문대예요. 경주에 가면 지금도 볼 수 있어요!”
- 배움 카드 표시 후 다음 문제 자동 진행

### 4.4 세트 / 라운드 / 게임 종료
- 세트 5문제 종료 → 현재 리더가 새 주제 선택 (세트 3개까지)
- 라운드 종료 → 라운드 점수 순위 표시, 방장이 **다음 라운드** (30초 후 자동 진행)
- 3라운드 종료 → 누적 점수 최종 순위(1·2·3위 시상대), 방장이 **다시 하기** → 대기실(점수 초기화)
- 다음 라운드 첫 리더 = 직전 문제의 리더를 그대로 유지

### 4.5 AI 펭수

| 상황 | 펭수의 행동 | 생성 방식 |
| --- | --- | --- |
| 게임 시작 / 주제 선택 | “펭-하!” 인사, 문제 준비 안내 | 기본 문장 |
| 오답 누적 | 격려 (“괜찮아! 틀리면서 배우는 거야”) | 기본 문장 |
| 아까운 오답 | 이름 불러 “거의 맞았어!” | 기본 문장 |
| 오답 5회마다 | 힌트 공개 | 출제 시 AI가 미리 생성 |
| 정답 | 정답자 이름을 불러 축하 | 정답 순간 OpenAI 호출 (실패 시 기본 문장) |
| 정답 공개 | 초등학생 눈높이 설명 (배움 카드 + 채팅 `📘 알고 가요!`) | 출제 시 AI가 미리 생성 |
| 시간 초과 / 라운드·게임 종료 | 위로·칭찬, “펭-바!” | 기본 문장 |

- 페르소나: 남극에서 온 10살 펭귄, EBS 연습생, 당당하고 유쾌한 반말, 절대 놀리지 않음
- 화면: 채팅 위 펭수 아바타 + 말풍선(최근 펭수 메시지), 채팅 속 `🐧 AI 펭수` 메시지
- 아바타 이미지: `public/pengsoo.png`

## 5. 점수

| 대상 | 점수 |
| --- | --- |
| 정답자 | 10점 − (공개된 힌트 수 × 2점) |
| 리더(그린 사람) | 누군가 맞히면 +5점 |
| 시간 초과 | 점수 없음 |

- 라운드 점수(라운드마다 초기화)와 누적 점수를 따로 관리
- 동점은 같은 순위

## 6. AI 제시어 생성

- 모델: OpenAI Chat Completions (`OPENAI_MODEL`, 기본 `gpt-4.1-mini`), JSON 응답 모드
- 입력: 주제, 난이도(학년), 이 방에서 이미 나온 제시어 목록(중복 방지)
- 출력: 제시어 5개, 각 제시어의 허용 별칭(띄어쓰기·동의어), 쉬운 순서의 힌트 3개, **정답 공개 때 보여줄 쉬운 설명(해당 학년 눈높이, 1~2문장, 존댓말)**
- 조건: 그림으로 표현 가능한 명사, 해당 학년 교과서 수준, 폭력·공포 등 부적절 단어 제외, 힌트에 정답 단어 직접 포함 금지
- 제시어 생성은 **OpenAI API**를 사용하며, 서버에서만 호출 (키는 브라우저에 노출하지 않음)
- 실패 대비: 오류/타임아웃(20초) 시 게임이 멈추지 않도록 내장 단어장에서 출제 (힌트: 짧은 힌트 → 첫 글자 → 초성, 설명 포함)

## 7. 실시간 동기화 설계

Vercel 서버리스 함수는 WebSocket을 유지할 수 없으므로 **짧은 주기 폴링 + Neon DB** 방식을 사용한다.

- 클라이언트는 `GET /state`를 그리기 중 0.6초, 그 외 1.2초 간격으로 호출 (이전 요청 완료 후 다음 요청)
- 증분 동기화: 마지막으로 받은 획(stroke) ID, 채팅 ID 이후 데이터만 수신
- 리더는 그린 좌표를 0.1초 단위로 묶어 순서대로 전송
- 획 데이터: 800×600 논리 좌표, `{s: 획번호, c: 색, w: 굵기, p: [[x,y],…]}`, `clear`, `undo` 이벤트
- 게임 진행(시간 초과, 다음 문제 등)은 별도 서버 없이 **폴링 요청 시점에 서버가 마감 시간을 확인해 전이** (DB 조건부 UPDATE + `version` 컬럼으로 중복 전이 방지)
- 서버 시각을 응답에 포함해 클라이언트 타이머 오차 보정

## 8. 데이터 모델 (Neon PostgreSQL)

모든 게임 상태(방·참가자·채팅·그림)는 **Neon**에 저장하며 `@neondatabase/serverless` HTTP 드라이버로 접근한다.

```
rooms     code PK, host_id, status(lobby|choosing|generating|drawing|reveal|round_end|game_end),
          version, round, set_no, question_no, seq, topic, difficulty, leader_id,
          words jsonb, current_word jsonb, hints_revealed, wrong_count,
          used_words jsonb, last_result jsonb, deadline, created_at, updated_at
players   (room_code, id) PK, name, score_total, score_round, joined_at, last_seen
messages  id PK, room_code, player_id, name, kind(chat|correct|system|hint|explain|pengsoo), text, created_at
strokes   id PK, room_code, seq, data jsonb
```
- 테이블은 첫 요청 시 자동 생성 (`CREATE TABLE IF NOT EXISTS`)
- 6시간 이상 활동 없는 방은 새 방 생성 시 정리

## 9. API

| 메서드 | 경로 | 설명 |
| --- | --- | --- |
| POST | `/api/rooms` | 방 생성 |
| POST | `/api/rooms/[code]/join` | 입장 |
| GET | `/api/rooms/[code]/state` | 상태·증분 획·채팅 조회 (+ 진행 전이) |
| POST | `/api/rooms/[code]/start` | 게임 시작 (방장) |
| POST | `/api/rooms/[code]/topic` | 주제·난이도 선택 (리더) |
| POST | `/api/rooms/[code]/strokes` | 획 전송 (리더) |
| POST | `/api/rooms/[code]/chat` | 채팅/정답 시도 |
| POST | `/api/rooms/[code]/next` | 다음 라운드 / 다시 하기 (방장) |
| POST | `/api/rooms/[code]/leave` | 나가기 |

- 인증 없음: 브라우저 `localStorage`의 무작위 `playerId`로 식별
- 제시어·힌트 원본은 리더와 정답 공개 이후에만 응답에 포함

## 10. 화면

1. **홈**: 게임 소개, 닉네임, [방 만들기], 방 코드 입력 + [참가하기]
2. **대기실**: 방 코드·초대 링크 복사, 참가자 목록(방장 👑), 게임 규칙 요약, [게임 시작]
3. **게임**: 좌측 출석부(참가자·점수) / 가운데 상단 진행 정보(라운드·주제·문제·벽시계 타이머) + 나무 액자 칠판 + 분필 받침대 도구 / 우측 AI 펭수 말풍선 + 알림장(공책) 채팅
   - 태블릿·모바일에서는 세로 배치, 캔버스 터치 그리기 지원
4. **주제 선택 모달**, **정답 공개 오버레이**, **라운드 순위**, **최종 순위(시상대)**

## 11. 비기능 요구사항

- 동시 방 수십 개, 방당 10명 기준 그림 지연 1초 이내
- 채팅 최대 60자, 닉네임 최대 10자, 입력값 서버 검증
- 초등학생 대상: 큰 글씨·버튼, 밝은 색감, 쉬운 안내 문구
- 개인정보 수집 없음 (닉네임 외 저장하지 않음)

## 12. 환경 변수

| 이름 | 필수 | 설명 |
| --- | --- | --- |
| `DATABASE_URL` | ✅ | Neon 연결 문자열 (Vercel Neon 연동 시 자동 등록) |
| `OPENAI_API_KEY` | ✅ | 제시어·힌트·설명 생성 (없거나 오류 시 내장 단어장 사용) |
| `OPENAI_MODEL` | 선택 | 기본 `gpt-4.1-mini` |
| `MIN_PLAYERS` | 선택 | 시작 최소 인원, 기본 3 (테스트 시 1~2로 조정) |
| `GAME_TIME_SCALE` | 선택 | 제한 시간 배율, 기본 1 (테스트용, 예: 0.2) |

## 13. 향후 확장

- WebSocket 기반 실시간 서비스(Pusher/Ably) 전환으로 지연 단축
- 교사 모드: 학급 전용 단어장 업로드, 특정 단원 지정
- 비속어 필터, 신고 기능
- 학습 리포트(맞힌 제시어·개념 요약)
